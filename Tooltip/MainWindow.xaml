<syncfusion:ChromelessWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tooltip"
    xmlns:skin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf" x:Class="Tooltip.MainWindow"
        mc:Ignorable="d"
        syncfusion:SkinStorage.MetroBrush="Black" Title="ToolTip" ResizeMode="CanResizeWithGrip" TitleBarForeground="White"  TitleBarBackground="CornflowerBlue"
                             skin:SfSkinManager.VisualStyle="Office2016Colorful" CornerRadius="0">

    <Window.Resources>
        <DataTemplate x:Key="tooltipTemplate1">
            <Border 
                    Name="Border"
                    Background="AliceBlue"
                    BorderBrush="Red"
                    BorderThickness="1" Width="60" Height="20"
                    CornerRadius="0"
                    >
                <TextBlock Background="Transparent" Text="{Binding Path=ToolTip}" Padding="2" />
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Name="grid">
        
    </Grid>
</syncfusion:ChromelessWindow>
